// ~~ Generated by projen. To modify, edit .projenrc.js and run "npx projen".
import * as path from 'path';
import * as lambda from 'aws-cdk-lib/aws-lambda';
import { determineLatestNodeRuntime } from 'aws-cdk-lib/aws-lambda';
import { Construct } from 'constructs';

/**
 * Props for DisableSecurityhubFunction
 */
export interface DisableSecurityhubFunctionProps extends lambda.FunctionOptions {
  /**
   * The Lambda runtime to use.
   * @default - Latest Node.js runtime available in the deployment region
   */
  readonly runtime?: lambda.Runtime;
}

/**
 * An AWS Lambda function which executes src/lambda/disable-securityhub.
 */
export class DisableSecurityhubFunction extends lambda.Function {
  constructor(scope: Construct, id: string, props?: DisableSecurityhubFunctionProps) {
    super(scope, id, {
      description: 'src/lambda/disable-securityhub.lambda.ts',
      ...props,
      runtime: props?.runtime ?? determineLatestNodeRuntime(scope),
      handler: 'index.handler',
      code: lambda.Code.fromAsset(path.join(__dirname, '../../assets/lambda/disable-securityhub.lambda')),
    });
    this.addEnvironment('AWS_NODEJS_CONNECTION_REUSE_ENABLED', '1', { removeInEdge: true });
  }
}